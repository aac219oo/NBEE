{
    "name": "monorepo-root",
    "private": true,
    "packageManager": "bun@1.3.5",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build": "turbo build",
        "dev": "turbo dev",
        "lint": "turbo lint",
        "format": "turbo format",
        "hive": "bun packages/hive/scripts/cli.ts"
    },
    "devDependencies": {
        "turbo": "^2.3.3"
    }
}